# 叠衣 Demo 关键帧诊断门槛

摩擦抓取测试不能只依据控制日志宣称成功。日志中的“夹爪附近存在布料点”只说明空间距离较近，不代表目标衣角进入了两指之间，更不代表闭合后夹住了正确布层。

## 每次运行的强制产物

`run_genesis_ipc.py --contact-grasp-test` 现在会自动生成：

- 主视频；
- 启用 `--record-multi-view` 时的四路同步视频与四宫格视频；
- 第一、第二、第三折状态快照（布料顶点、两侧 TCP、夹爪指令）；
- 三折完整生命周期的四视角关键帧图片；
- `first_fold_contact_sheet.png`、`second_fold_contact_sheet.png`、
  `third_fold_contact_sheet.png` 总览；
- 每次闭合事件的布料补丁随动 PASS/FAIL。

四个视角分别是整体斜视、俯视、从短袖下侧看向领口，以及跟随右 TCP 的抓取近景。

## 验收顺序

1. 第一折看 45/54、90/117 两次闭合，再看 150/270/300/332 的抬升、放置与释放。
2. 第二折看 385/393、430/439：两手闭合时目标布层是否真的位于两指之间。
3. 看 455/480/530/550/570/583：两侧折片是否从上方叠放，而不是平齐侧撞或拖动主体。
4. 第三折看 650/680/690 的当前腰摆抓点，再看 720--960 的抬升、翻折、水平放置和释放。
5. 视觉通过后，再读 `contact_grasp_summary`：补丁随动投影、方向余弦、与 TCP 的距离增长必须同时合理。
6. 状态快照必须结合 garment atlas 的衣服局部分区，不能再用相机左右或单一 world-X 分位数代替布片语义。

只有视觉检查和数值检查都通过，才能把该次抓取标记为有效。

统一状态为 `FAIL / INCOMPLETE / REVIEW / PASS / NOT_RUN`。自动检查无硬错误时仍是
`REVIEW`；只有填写对应运行的 `manual_review.json` 后才可能成为 `PASS`。

## 网格规则

- 2500 面只用于代码冒烟测试，不用于轨迹和抓取结论。
- 日常抓取调试使用 8000 面。
- 最终结果再用原始约 14000 面模型复核。
- 不跨不同网格分辨率直接比较衣角位置、褶皱或抓取成功率。
