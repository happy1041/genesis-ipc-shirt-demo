# 叠衣 Demo 定量交互约定

## 1. 统一描述坐标

- 一律以穿着者视角的短袖为参考，不使用屏幕左右或机器人左右。
- `短袖左/右`：穿着者的左袖/右袖方向。
- `短袖上/下`：领口方向/腰部方向。
- `抬高/降低`：垂直桌面向上/向下。
- 位移统一用毫米；时间优先用源轨迹帧号。

## 2. 一条反馈的最小格式

```text
阶段@帧号  对象：方向和距离；验收目标
```

例如：

```text
第二折@570  右侧落点：向短袖右侧 30 mm；释放后两折片覆盖率至少 70%
第三折@960  双夹爪：向短袖上侧 25 mm、抬高 15 mm；衣身水平漂移小于 20 mm
```

如果不知道毫米，可以用衣服尺寸作比例：`0.1 个衣身宽度`、`半个夹爪宽度`。诊断程序再换算成毫米。

## 3. 每折固定验收量

1. `抓取命中`：左右夹爪各自是否夹到目标布层，PASS/FAIL。
2. `主体漂移`：从闭合前到释放后的衣身核心区水平位移中位数，单位 mm。
3. `折片覆盖率`：目标折片投影落在目标区域内的比例，0–100%。
4. `释放保持率`：释放后 20 帧仍留在目标区域的折片比例，0–100%。
5. `视觉评分`：抓取、轨迹、落点、最终形状分别 0–5 分。

## 4. 单变量实验记录

每次只允许修改一个控制量，并使用如下记录：

```text
基线 checkpoint：G2/G3
改动：second_fold_stack_overlap 100 -> 70 mm
预期：第二折落点远离机械臂端 30 mm
结果：主体漂移 __ mm；覆盖率 __%；保持率 __%；人工评分 __/5
结论：保留 / 回退 / 在 __ 到 __ mm 之间二分
```

当前人工验收基线由 `outputs/episode_000000/checkpoints/ACTIVE_STAGE_CHECKPOINTS.json` 唯一指定，避免把技术性临时 checkpoint 误当成正式入口。

## 5. 按帧查询机械臂 TCP

后续运行会自动生成 `*_tcp_trajectory.csv`，它记录修正后实际执行的每一帧，
而不只是原始 SIM1 关节指令。查询命令：

```bash
/home/happy1041/work/genesis-ipc-env/bin/python \
  diagnostics/query_tcp_frame.py \
  --run-json outputs/episode_000000/某次运行.json \
  --frame 570 --window 2
```

`--window 2` 会同时显示 568--572 帧；改成 `0` 就只看指定帧。输出坐标和
相邻帧位移均会换算成毫米。旧运行没有逐帧 CSV 时，工具会自动退回已有的
关键帧 `.npz`；因此 V2 可以查询 332、385、393、405、415、430、439、455、
480、505、530、550、570、583、600、619 等已保存帧。

原始 SIM1 轨迹与 Genesis 修正轨迹必须区分：

- `analyze_trajectory_tcp.py`：读取原始 `joint_q` 并通过 URDF 正运动学计算 TCP；
- `*_tcp_trajectory.csv`：记录 Genesis 经过 IK 修正后真正执行的 TCP；
- 运行日志中的 `frame=N/M` 主要是进度，`contact_track frame=...` 才是抓取诊断，
  二者都不适合作为完整的逐帧位置表。
