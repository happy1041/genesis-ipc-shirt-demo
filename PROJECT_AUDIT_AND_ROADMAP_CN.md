# Genesis IPC 叠衣项目总审计与路线图

## 一句话结论

项目已经完成“官方 SIM1 资产与轨迹可在 Genesis IPC 中回放、自然摩擦抓取可工作、前两折可形成”的工程底座，但尚未完成“可重复、可量化、最终形状合格的三折 Demo”。当前最高收益不再是继续盲调单个高度，而是先固定初态、给网格增加衣服语义、按折分段验收，并让第三折抓点根据当前布料状态更新。

## 当前系统由什么组成

当前 SIM1 唯一规范根目录为 `/home/happy1041/Workspace/SIM1`；运行脚本、资产解析和
离线 provenance 均应优先使用该目录。旧运行 JSON 中保留的 `/media/.../SIM1` 只作为
历史记录，诊断器会显式记录旧路径到新路径的重定位，不能再作为新实验输入。

| 层 | 当前来源 | 已验证 | 主要风险 |
|---|---|---|---|
| 机器人资产 | SIM1 `assets/acone/acone.urdf` | 19 维关节映射、夹爪开闭方向、TCP 链路 | Genesis IPC coupler 是本地修改版，需固定版本 |
| 衣服资产 | SIM1 `short-shirt.usdc` 导出的同一短袖网格 | 2500/8000/约 14000 面三级网格可用 | 降模会改变抓取区拓扑；不同面数不能直接比较 |
| 轨迹 | SIM1 episode 000000，60 Hz | 第一、二、三折事件时序已恢复 | 轨迹是开环的，前序微小误差会让后续固定抓点失配 |
| 物理 | Genesis `FEM.Cloth` + libuipc 接触 | 自碰撞、桌面接触、实体夹爪摩擦抓取已跑通 | 与 SIM1 的 Newton/VBD 本构、接触和初态不同 |
| 运行策略 | 机械臂按记录 qpos 作为规定运动边界 | 便于做同轨迹后端对比 | 不是动力学机器人控制，也不会自动纠正布料状态 |
| 诊断 | 四视角视频、关键帧、布料 NPZ、抓取随动日志、checkpoint | 可定位夹空、单层滑脱、整衣拖动 | 旧结论散落，自动指标尚不能替代视觉判定 |

## 2026-08-14 停机审计结果

本轮没有启动仿真，只对当前完整 8000 面三折结果
`genesis_ipc_three_folds_arc_lift80_top40_8000f_save.json` 做离线重算。总状态是
`FAIL`，而不是“前两折已可冻结、只剩第三折”。具体证据如下：

- **G0 provenance：PASS。** 轨迹、短袖网格、Acone URDF、runner 和本地修改过的
  Genesis IPC coupler 均有 SHA-256；Genesis commit 为 `8b1dba2`，SIM1 commit 为
  `973476b`。项目目录自身不是 Git 仓库，因此 runner/coupler diff 哈希是必要证据。
- **G2 第一折：INCOMPLETE。** 这个旧运行没有保存新增的第一折完整四视角关键帧，
  所以即使肉眼曾认为可用，也不能把它升级为批准 checkpoint。这里不是自动判定
  第一折失败，而是证据不足。
- **G3 第二折：FAIL。** 393→583 帧中央主体的累计平面中位位移为 **137.8 mm**，
  明显超过 15 mm 护栏；583 帧第一、第二折片的网格占用覆盖仅 **5.14%**，IoU
  **2.05%**。后一个数值仍需用衣服语义和多视角人工复核，但足以否决当前
  post-second checkpoint。
- **G4 第三折：FAIL。** 右夹爪 690 帧闭合后，跟踪布片相对 TCP 的距离增长
  **48.8 mm**，说明夹持未稳定保持；最终三宽度带共同投影覆盖仅约 **1.43%**，且
  主体移动/释放形态护栏失败。

因此目前**没有一个可批准并向后传播的 checkpoint**。下一次仿真应先重建带完整
关键帧证据的 G2 候选，而不是从旧 post-second 状态继续调第三折。新增 checkpoint
格式会携带 frame 0 参考布料状态，并在后缀启动时立即保存来源帧，避免分段运行再次
丢失关键视觉与位移基线。

现有结果中最值得保留的候选链是：

1. `genesis_ipc_post_first_fold_narrow30_8000f_table1_v1`：两次抓取随动均为 PASS，
   且人工曾认可第一折形态；但旧版只保存了 332 帧，缺少第一折全套关键帧，所以它是
   **G2 重跑模板**，不是已批准 G2 checkpoint。
2. `genesis_ipc_second_fold_narrow30_lift_transfer_place60_8000f_v1`：第二折左右抓取随动
   均为 PASS，四视角帧齐全；但它从旧 checkpoint 启动，缺 frame 0/332 数值基线，
   `second_fold_motion_summary` 与叠层指标为空，而且人工观察到放置高度/层间碰撞问题。
   因此它只保留为**轨迹参数参考**，不能直接升级为 G3。

换言之，下次并非从零开始：先按 narrow30 的相同参数重跑一次第一折并补证据；G2
批准后，再复用 lift-transfer-place60 的第二折参数做单一候选，届时新 checkpoint
会让位移和叠层指标可计算。

## 已经完成与尚未完成

已完成：

- Genesis 场景、GPU IPC、Acone 双臂、桌面和短袖资产运行；
- 官方轨迹关节名映射和夹爪符号修正；
- 不使用锚点的实体夹爪摩擦抓取；
- 第一折和第二折的高位进场/搬运/放置轨迹改造；
- 8000 面调试网格、约 14000 面最终网格和 checkpoint 链路；
- 四视角同步视频与关键帧状态导出；
- 抓取布片是否随 TCP 运动、主体是否整体移动等数值护栏。
- 8000 面短袖 garment atlas：宽度带、领口/腰摆、桌面朝上/朝下表面和成对关键点；
- 离线 provenance、语义状态分析、Gate 评价、人工复核模板和 30 次历史运行索引。

尚未完成：

- Newton 接触解穿透后的布料状态没有被严格迁移，Genesis 仍从更高位置重新沉降；
- 第一折、第二折还没有被正式冻结为“验收通过”的可复用 checkpoint；
- 第三折仍会发生抓层/抓点失配、推布、竖直放置和释放回弹；
- 没有目标折后形状、衣服关键点和前后表面语义，因此现有 world-X 分区只能排查大错；
- 还没有在约 14000 面模型和多个种子上验证可重复性；
- 项目目录自身没有 Git 基线，Genesis coupler 有本地修改，旧运行难以完全复现。

## 根因优先级

1. **初态不一致。** SIM1 公开配置让短袖与桌面初始相交后由 Newton 解开，Genesis 为满足 IPC 无穿透约束从 `z=0.93` 重新沉降。两者会得到不同的前后层间距、褶皱和残余应力。这会直接改变后续固定抓点。
2. **开环轨迹与可变形状态不匹配。** 前两折有几厘米级的形状或质心偏差后，第三折仍使用固定世界坐标，抓取自然会偏离当前腰摆中心。继续手工修改单个 world offset 只能适配一次结果。
3. **“折”仍像平移后放下。** 抓点过高或双臂拉得过紧会拖动主体；过低会与已折折片侧撞；末端保持竖直会把折片立起来。更合理的是松弛抬起、滚动/弧形翻折、接近水平放置、短暂停留后释放。
4. **缺少布层语义。** 仅检查夹爪附近有布料，无法证明正面、背面以及两侧折片都进入指间。原 OBJ 的局部坐标其实可用于建立左/中/右、领口/腰摆、上表面/下表面的稳定顶点标签，应尽快利用。
5. **材料参数尚未校准。** `E`、面密度、弯曲刚度、布布摩擦和布桌摩擦会影响回弹与拖动，但在初态和轨迹几何未稳定前大范围扫参会把因果混在一起。
6. **实验治理不足。** 约 2 GB 产物、多个入口脚本和旧绝对路径并存；没有代码/资产哈希时，同名“8000 面结果”不保证来自同一 coupler 和同一参数。

## 两条路线必须分开

### A. 物理后端公平对比

- 完全保留 SIM1 轨迹；
- 从 SIM1 replay 的 USD 导出“接触已解穿透”的同一帧布料顶点，作为 Genesis 初态；
- 不引入状态自适应抓点；
- 比较 Newton 与 Genesis 的关键点轨迹、形变、接触稳定性和最终形状。

这条线回答“同轨迹换物理后端有什么差异”，即使折叠失败也有研究价值。

### B. 成功导向的 Genesis Demo

- 保留同一机器人、短袖资产和三折任务结构；
- 每一折开始时从当前衣服状态估计边缘/关键点；
- 对进场高度、滚动路径、放置姿态和释放停留做状态相关修正；
- 以任务完成度和最终折后形状为目标。

这条线回答“怎样在 Genesis IPC 中把衣服真正折好”。建议当前以 B 为主交付，同时保留 A 作为物理比较基线，不能再把两类结果写成同一个“同轨迹复现”。

## 新的阶段门槛

每次运行只允许验证一个假设，并按以下 Gate 逐级推进：

| Gate | 必须满足 | 失败后只允许调整 |
|---|---|---|
| G0 可复现性 | 代码、轨迹、网格、URDF、Genesis/SIM1 commit 与本地 diff 有哈希 | 环境/路径/版本，不调物理 |
| G1 初态 | 沉降后无爆炸/穿透；质心、边界、关键点和层间距稳定 | 初始状态迁移、接触阈值、settle |
| G2 第一折 | 双层抓取；主体不被拖走；折片落到目标区；释放后稳定 | 第一折抓点、净空、弧线、放置姿态 |
| G3 第二折 | 双臂都抓中；不擦碰第一折；折片叠到第一折上 | 第二折同类轨迹参数，不碰第三折 |
| G4 第三折 | 当前状态下抓住腰摆多层；主体不过度移动；水平放置并稳定释放 | 状态自适应抓点、第三折路径/姿态 |
| G5 最终验收 | 约 14000 面、至少 3 次运行；形状覆盖/IoU、厚度、稳定时间合格 | 小范围材料校准或闭环恢复 |

状态只有五种：

- `FAIL`：已有证据证明失败；
- `INCOMPLETE`：缺日志、状态或关键帧；
- `REVIEW`：自动护栏未发现硬错误，但还需人工视觉复核；
- `PASS`：数值和人工关键帧都通过；
- `NOT_RUN`：本次没有运行到该阶段。

前一 Gate 不是 PASS，就不创建“批准 checkpoint”，也不进入下一折调参。

## 自动诊断闭环

新工具位于 `diagnostics/`：

```bash
./diagnostics/run_diagnostics.sh outputs/episode_000000/<run>.json
```

它会：

1. 记录输入文件 SHA-256、SIM1/Genesis Git 状态、Genesis coupler diff 哈希、Python 包和 GPU；
2. 检查每一折应有的抓取事件、数值摘要和四视角关键帧；
3. 对蓝色布料做保守的图像掩码测量，只用于发现明显的位移/面积异常；
4. 生成 `manual_review.json`，要求按衣服自身方向逐项看图；
5. 生成 `evaluation.json/.md`，只有人工复核后才可能 PASS；
6. 更新整个输出目录的 `experiment_index.csv/.md`。

推荐实验命名：

`g<gate>_<single-hypothesis>_<mesh>f_seed<seed>_<date>`

禁止在同一次运行同时改“抓点 + 轨迹 + 摩擦 + 网格”。建议一次只改一个参数族，并保留基线与候选的关键帧接触表，不必每次都看完整视频。

## 网格与运行成本策略

- 2500 面：只验证代码能否启动、checkpoint 是否可加载，不对抓取与折叠下结论；
- 8000 面：所有轨迹、抓层和参数调试的标准网格；
- 原始约 14000 面：只在 G2--G4 已于 8000 面通过后做最终验证；
- checkpoint：只保存已通过 Gate 的状态；checkpoint 必须携带源运行、网格/轨迹/代码哈希和验收报告；
- 完整 980 帧运行：只在三段都分别通过后执行，日常调试只跑当前折的后缀。

## 可利用的外部资源

| 资源 | 值得借鉴 | 不应直接照搬 |
|---|---|---|
| SIM1 官方 replay/USD | `acone_cloth_manipulation.py` 每帧把 `state_0` 写入 USD；可提取同一轨迹的完整布料状态，最适合迁移接触解穿透后的初态和建立 Newton 参照关键点 | 其 `filter_cloth_quality.py` 主要做高度/边界异常过滤，不等于折衣完成度 |
| SIM1 任务信号与视频判别器 | 把长轨迹分成抓取/折叠事件；可借鉴视频级成功分类器结构 | 当前没有确认可直接用于本任务的训练权重 |
| Genesis IPC 官方夹布示例 | 实体夹爪接触、IPC 参数和控制方式的最小参考 | 官方 IPC 接口仍较新，示例不是长时序多层折衣基准 |
| GarmentLab | 衣服资产、任务组织和 IoU/覆盖率式评价 | 基于 PhysX，多数资产不能当作同资产后端对比 |
| FoldNet / ClothFunnels | 衣服关键点、固定折叠启发式、闭环恢复数据 | 目标是策略学习，不会自动解决本项目 IPC 参数问题 |
| IPC / C-IPC | 无交叉接触、厚度、摩擦和多层布料的理论参照 | “无穿透”不等于“会形成真实持久折痕”，性能也不是必然实时 |

## 建议的下一步顺序

1. 先运行离线诊断器整理现有 30 余个 JSON，不启动仿真，选出真正可作为 G2 候选的第一折结果。
2. 使用已生成的 8000 面 garment atlas 校准一次穿着者左/右符号，并用一条人工认可结果标定投影覆盖阈值；不再新增另一套临时 world-X 标签。
3. 从 SIM1 USD 导出 Newton 解穿透后的布料状态，建立 G1 的公平初态；同时保留当前 Genesis 自沉降初态做消融。
4. 用 8000 面重跑一次第一折并正式人工验收，通过后保存新的 G2 checkpoint。
5. 只从 G2 checkpoint 调第二折；通过 G3 后保存 post-second checkpoint。
6. 第三折不再长期依赖固定 world offset：先在当前状态中估计腰摆中点与局部层数，再生成“松弛抬起—弧形翻折—水平放置—停留—释放”的短轨迹。
7. 路径稳定后才做小规模材料消融（建议每次只扫一个变量，3 个值），最后用约 14000 面和至少 3 次重复验证。

## 当前默认决策

在没有额外要求前，默认把“成功导向 Genesis Demo”作为主线，把“严格同轨迹后端对比”保留为对照线。最终成功暂定义为：三折完成、关键布层抓取正确、释放后静置 1 秒不明显回弹、俯视目标区域覆盖合格且没有大面积布料竖立。覆盖阈值需要从一帧人工认可的 SIM1/Genesis 目标状态标定，而不是现在凭感觉拍一个数字。
