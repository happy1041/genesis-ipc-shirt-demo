# 8000 面 IPC / dHat / 轨迹调试记录

日期：2026-08-15

## 当前基线

- 短袖：`short-shirt-8000f.obj`
- IPC：fast solver
- `contact_d_hat = 0.002 m`
- 初始静置：60 帧
- 第二折：`smooth_arc`，圆弧峰高 `0.040 m`
- 原始 SIM1 Acone URDF

以上参数已写入 `run_g3v2_full_fast_8000.sh` 的默认值，同时仍可通过同名环境变量覆盖。

## dHat 与静置结论

静置 30 帧时，布料相对初始面的高度分布：

| dHat | 中位数 | P90 | 高于 20 mm 的顶点 |
|---:|---:|---:|---:|
| 5 mm | 15.9 mm | 36.7 mm | 41.1% |
| 2 mm | 4.6 mm | 16.5 mm | 5.6% |
| 1 mm | 2.5 mm | 14.2 mm | 3.4% |

1 mm 虽然更薄，但第二折摩擦抓取的稳定性下降；2 mm + 60 帧静置是当前更可靠的折中。继续增加静置时间不能消除由接触距离/初始网格造成的剩余起伏。

## IPC 可视化

关键帧中橙色线框是实际送入 IPC 的刚体碰撞代理，灰色模型是渲染/目标姿态。每帧同时记录：

- 刚体代理与目标的平移、旋转误差；
- 代理相对桌面的最低净空；
- 夹爪闭合后邻域布料是否随 TCP 同向运动。

当前配置启用了布料—刚体和布料自碰撞，但没有启用刚体—刚体 IPC。因此，IPC 会阻止布料穿过夹爪，却不会让桌面阻止机器人穿过桌面。第一折约 90--117 帧出现的约 31--32 mm 桌面穿越属于目标轨迹/接触对配置问题，不是继续减小 dHat 能解决的问题。

## 第二折轨迹 A/B

两版均通过左右手摩擦抓取跟随检查，且折片叠层检查通过。

| 圆弧峰高 | 中部 35--65% 中位/P90 | 深层核心中位/P90 | 第一/第二折片覆盖率 |
|---:|---:|---:|---:|
| 60 mm | 18.1 / 140.9 mm | 8.6 / 37.0 mm | 66.4% |
| 40 mm | 21.8 / 106.4 mm | 6.9 / 14.4 mm | 65.3% |

40 mm 版对折叠边界附近的扰动略大，但显著降低真正衣身核心区的整体拖动，只损失约 1.1 个百分点的折片覆盖率，因此选为当前默认。

## 诊断规则

- `stationary_body`：初始世界 X 的 35--65 分位，继续作为严格的拖动告警。
- `deep_core`：初始世界 X 的 45--55 分位、Y 的 30--70 分位，用来区分“折叠边界被正常带走”和“整件衣服平移”。
- 折片重叠必须同时检查平面覆盖率和多视角关键帧；单看质心或日志不足以判定折叠成功。

## 下一步

1. 单独处理第一折抓取阶段的桌面穿越：评估侧向进入轨迹或启用刚体—刚体接触，不能只整体抬高夹爪，否则会失去桌面上的布边。
2. 以当前 40 mm 第二折为 checkpoint 继续第三折；保持 dHat、静置和材料参数不变，避免同时改变多类变量。
3. 只有 8000 面全流程稳定后，再将同一诊断流程迁移到 13767 面网格。
